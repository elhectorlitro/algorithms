import random

def GenerateRandomList(Mini, Maxi, Num_of_elements):
  ran_list = []
  for i in range (0,Num_of_elements):
      ran_value = random.randint(Mini,Maxi)
      ran_list.append(ran_value)
  return ran_list

def SelectionSort(UnSortList):
  Copy = []

  return 0

print(GenerateRandomList(2,9,3))


